# exercise 1 rectangle area calc

length = float(input("Enter the length: "))
width = float(input("Enter the wideth: "))
area = length * width

print(f"The area is: {area}cm²)") # superscript for mac: control + command + Space
                                  # superscript for windows: NumLock + Alt + 0178
